@extends('layouts.base_layout')

@section('title', 'SellOn - Marketplace Eksklusif Kampusmu')

@section('content')

{{-- 
VARIABLE:
$products = Array variable about all the data inside 'Product' table
--}}
<main class="max-w-7xl mx-auto lg:mx-50 px-4 sm:px-6 lg:px-8 py-8 min-h-screen bg-brand-secondary">
  <div id="home-view" class="view-section">
    <div class="bg-brand-main rounded-2xl p-8 md:p-12 mb-10 text-center text-white relative overflow-hidden">
      <div class="relative z-10">
        <h1 class="text-4xl md:text-5xl font-display font-bold mb-4">Marketplace Eksklusif Kampusmu</h1>
        <p class="text-brand-muted text-lg mb-8 max-w-2xl mx-auto">Jual beli aman dan nyaman khusus untuk civitas
          akademika. Mulai cari kebutuhan kuliahmu sekarang.</p>
        <a href="{{ route('product.index', ['view_type' => 'products.create_form']) }}" wire:navigate class="btn btn-primary text-lg inline-block">Mulai Berjualan</a>
      </div>
    </div>
    <h2 class="text-2xl font-display font-bold mb-6 text-brand-main">Kategori Populer</h2>
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-10">
      <div
        class="bg-white p-4 rounded-xl shadow-sm text-center border border-slate-100 hover:border-brand-accent cursor-pointer transition">
        <div
          class="w-12 h-12 bg-indigo-50 rounded-full mx-auto mb-3 flex items-center justify-center text-brand-accent">
          📚</div>
        <p class="font-medium">Buku & Modul</p>
      </div>
      <div
        class="bg-white p-4 rounded-xl shadow-sm text-center border border-slate-100 hover:border-brand-accent cursor-pointer transition">
        <div
          class="w-12 h-12 bg-indigo-50 rounded-full mx-auto mb-3 flex items-center justify-center text-brand-accent">
          💻</div>
        <p class="font-medium">Elektronik</p>
      </div>
      <div
        class="bg-white p-4 rounded-xl shadow-sm text-center border border-slate-100 hover:border-brand-accent cursor-pointer transition">
        <div
          class="w-12 h-12 bg-indigo-50 rounded-full mx-auto mb-3 flex items-center justify-center text-brand-accent">
          🛵</div>
        <p class="font-medium">Kendaraan</p>
      </div>
      <div
        class="bg-white p-4 rounded-xl shadow-sm text-center border border-slate-100 hover:border-brand-accent cursor-pointer transition">
        <div
          class="w-12 h-12 bg-indigo-50 rounded-full mx-auto mb-3 flex items-center justify-center text-brand-accent">
          🏠</div>
        <p class="font-medium">Info Kos</p>
      </div>
    </div>

    <div class="flex justify-between items-end mb-6">
      <h2 class="text-2xl font-display font-bold text-brand-main">Rekomendasi Untukmu</h2>
      @if($products->isNotEmpty())
        <a href="{{ route('product.catalog') }}" wire:navigate class="btn btn-secondary py-2! px-4! text-sm">Lihat Semua</a>
      @endif
    </div>

    @include('partials._square_product_card', ['products' => $products])
  </div>
</main>

@vite('resources/js/home_js.js')

@endsection